## Do not edit!
## File managed by saltstack!
{% set cfg = pillar.get('dovecot:config', {}) -%}
{#- Set some useful MACRO -#}
{%- from "dovecot/macros.jinja" import get_config with context -%}

{{ get_config('mail_plugins', '$mail_plugins notify replication', parentpillar='dsync') }}

# Other requirements:
#
# set service aggregator, replicator and doveadm

{{ get_config('doveadm_password', parentpillar='dsync') }}
{{ get_config('replication_max_conns', parentpillar='dsync') }}

plugin {
  #mail_replica = tcp:anotherhost.example.com:12345 # use port 12345 explicitly
  {{ get_config('mail_replica', parentpillar='dsync') }} 
}
